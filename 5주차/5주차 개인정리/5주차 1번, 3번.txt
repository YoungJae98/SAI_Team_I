1번
O
X (training set을 너무 늘리면 과적합 문제에 빠질 수 있다.)
X (반대)
O (애매한데... 학습은 계속 다르게 하더라도 동일한 test set으로 여러번 성능을 평가하는 방식이니
    여러번 사용할 수 있다가 맞다고 생각한다.
O

3번
틀린것
c : 낮아진다.
b : nomalization에서는 너무 큰값들을 가지는 값으로 인해
    단순히 성능을 체크할때 어긋날 수 있기때문에
    일정 범위 내로 값을 조정하는 것이다.
    outlier는 학습에 사용되서는 안되는 잘못된 값으로 이상치라 불린다.
    이상치는 주로 학습에서 제외하는 방식으로 다루기에 nomalization은
    outlier treatment에 속하지는 않는다.